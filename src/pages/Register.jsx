import Form from "../component/Form";

function Register(){
    return(
        <div>
            <Form action='register' textBtn={`Create Account now..`} />
        </div>
    )
}

export default Register;